{
  "digestVersion": "v1",
  "purpose": "Sanitized orchestration digest; not an importable workflow export.",
  "inputSchema": "../schemas/request.schema.json",
  "resultSchema": "../schemas/result.schema.json",
  "nodes": [
    { "role": "trigger", "action": "accept versioned delivery" },
    { "role": "service-call", "action": "call deterministic service with timeout" },
    { "role": "retry", "action": "retry retryable timeout at most twice with the same deliveryId" },
    { "role": "approval", "action": "wait for human approval after completed result" },
    { "role": "error-route", "action": "receive invalid-input, delivery-ID conflict, timeout, or partial-failure outcome" }
  ],
  "redaction": [
    "authentication material removed",
    "private endpoints removed",
    "instance identifiers removed",
    "customer data removed"
  ]
}
