{
  "name": "@arcitai/solution-template",
  "version": "0.2.1",
  "description": "AI-native architecture and delivery guidance for applications, services, automations, integrations, and systems.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "format:check": "prettier --check \"**/*.{md,json,yml,yaml,mjs}\"",
    "lint:markdown": "markdownlint-cli2 \"**/*.md\" \"#node_modules\"",
    "lint:links": "node scripts/check-links.mjs",
    "lint:yaml": "node scripts/check-yaml.mjs",
    "check": "npm run format:check && npm run lint:markdown && npm run lint:yaml && npm run test && npm run lint:links"
  },
  "devDependencies": {
    "markdown-link-check": "3.14.2",
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6",
    "yaml": "2.9.0"
  }
}
